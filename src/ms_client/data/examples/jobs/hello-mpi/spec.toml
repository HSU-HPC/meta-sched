cmd_setup_target = "mpicxx $MS_INPUT/count.cpp -o count"
cmd_main = "mpiexec ./count"
nodes = 2
ranks_per_node = 1
required_modules = ["MPI"]
time       = "=p+10" # Takes one second per MPI rank (+10 buffer)